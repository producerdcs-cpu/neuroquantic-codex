const GraphVisualizer={render(containerId='graphCanvas'){const el=document.getElementById(containerId);if(!el||!window.CodexGraph)return;const edges=CodexGraph.all(),nodes=[...new Set(edges.flatMap(e=>[e.from,e.to]))];if(!nodes.length){el.innerHTML='<div class="graph-empty">O mapa está vazio.<br><small>Crie relações entre Códices para revelar a arquitetura.</small></div>';return}const w=760,h=Math.max(300,Math.ceil(nodes.length/3)*120),gapX=w/3,pos=Object.fromEntries(nodes.map((n,i)=>[n,{x:gapX*(i%3)+gapX/2,y:70+Math.floor(i/3)*120}]));const lines=edges.map(e=>{const a=pos[e.from],b=pos[e.to];return `<line x1="${a.x}" y1="${a.y}" x2="${b.x}" y2="${b.y}" class="graph-edge"/><text x="${(a.x+b.x)/2}" y="${(a.y+b.y)/2-6}" class="graph-label">${e.type}</text>`}).join('');const circles=nodes.map(n=>`<g><circle cx="${pos[n].x}" cy="${pos[n].y}" r="34" class="graph-node"/><text x="${pos[n].x}" y="${pos[n].y+4}" text-anchor="middle" class="graph-node-text">${n}</text></g>`).join('');el.innerHTML=`<svg viewBox="0 0 ${w} ${h}" role="img" aria-label="Mapa visual das relações entre Códices">${lines}${circles}</svg>`}};window.GraphVisualizer=GraphVisualizer;
