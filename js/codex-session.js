const CodexSession={version:'0.8.3',key:'nqc_codex_sessions_v1',read(){try{return JSON.parse(localStorage.getItem(this.key)||'[]')}catch{return[]}},write(items){localStorage.setItem(this.key,JSON.stringify(items));return items},open({mode='codex',codename='',input='',language='pt-BR'}={}){const now=new Date().toISOString(),state=window.ExperienceState?.create?.(mode,input)||null;const codex={id:`CODEX-${Date.now()}`,version:this.version,codename:String(codename||'Codex Anônimo'),mode,input:String(input||''),language,experienceState:state,status:'open',createdAt:now,updatedAt:now};const items=this.read();items.push(codex);this.write(items);return codex},save(codex){if(!codex?.id)return null;codex.updatedAt=new Date().toISOString();const items=this.read(),i=items.findIndex(x=>x.id===codex.id);if(i<0)items.push(codex);else items[i]=codex;this.write(items);return codex},get(id){return this.read().find(x=>x.id===id)||null},list(limit=20){return this.read().slice(-limit).reverse()},close(id){const c=this.get(id);if(!c)return null;c.status='closed';c.updatedAt=new Date().toISOString();return this.save(c)},status(){return{version:this.version,stored:this.read().length,ready:true}}};window.CodexSession=CodexSession;
